---
const { bannerItem } = Astro.props;
---

<div class="sub-banner" style={{ backgroundImage: `url(${bannerItem.url})` }}>
  <div class="inner">
    <div class="wrap">
      <article>
        <p class="sub-banner-tit f-pop">{bannerItem.title}</p>
      </article>
      <article>
        <p class="sub-banner-txt f-pop">
          {bannerItem.text}
        </p>
        <!-- <img class="" src=`${bannerItem.img}` alt="" /> -->
      </article>
    </div>
  </div>
</div>
